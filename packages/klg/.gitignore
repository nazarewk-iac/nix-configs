# ignore everything by default except directories and .gitignores
#  note: it wouldn't look inside of any directory otherwise
#  note: you can debug ignores using git check-ignore -v testfile.json
*
!*/
!/.gitignore

!*.md
!*.nix
!*.py
!/.envrc
!/flake.lock
!/flake.nix
!poetry.lock
!pyproject.toml
